package gold.debug.windowstolinux.app.ui.setting;

/**
 * Settings apply immediately; there is no unsaved page-local value to retain.
 *
 *  <p>设置立即生效，因此没有需要保留的未保存页面局部值。
 */
public record SettingPageState() {
}
