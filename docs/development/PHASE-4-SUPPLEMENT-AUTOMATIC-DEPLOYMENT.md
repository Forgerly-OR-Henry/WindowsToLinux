# WindowsToLinux 四期补充开发文档：面向新手的一体化自动部署

## 文档信息

- 文档版本：`1.0.0-doc-naming`
- 文档角色：四期补充开发文档，记录新手界面、一体化自动部署、AI 补全与 DB 管理
- 文档状态：本地实现与回归完成；自动 UI、真实 AI 与 DB 的组合验收仍为 `RUNTIME-PENDING`，详见第 5 节；各语言的 SSH 产品入口部署见 [Ubuntu 24 实机记录](UBUNTU-24-LIVE-DEPLOYMENT-2026-09-08.md)
- 更新日期：2026-09-08
- 上级文档：[四期工程细化文档](PHASE-4.md)
- 开发总纲：[DEVELOPMENT.md](DEVELOPMENT.md)
- 结构规范：[项目文件结构](../File.md)

## 1. 产品目标和实施约束

沿用现有 Java 21、Swing、FlatLaf、主题和中英文机制，将桌面工具改为面向新手的一体化自动程序。用户选择源码和服务器后，一次启动完成常规部署，不再逐步点击分析、检查、环境准备和发布。单应用与多组件共用首页及现有部署事务、失败恢复和持久化机制。本次不开发 Web 前端。

执行顺序必须是：本补充文档定稿及引用核对 → 公共界面与页面简化 → 自动部署编排 → AI 补全 → DB 管理 → 完整回归。代码开发前同步四期主文档、开发总纲、产品说明书及结构目录。保留开发总纲移入本目录的已有工作区变动。进度以 `.ai-workspace/progress/CURRENT.md` 为准；未通过验证的能力不得标记已实现或实机通过。

## 2. 默认界面与高级侧栏

### 2.1 部署首页

- 上方左侧为源码选择：本地目录或 Git 地址；中间显示部署方向；右侧选择已保存服务器，支持就地添加。
- Git 默认解析仓库默认分支并固定到具体提交；分支、标签、提交覆盖放在高级侧栏。所有组件使用同一次源码快照。
- 添加服务器主要填写地址、用户名和密码；SSH 端口默认 22。保存资料不等于主机已受信。
- 只保留一个“一键部署”主按钮，单应用与多组件均自动识别并执行，不暴露必须逐个点击的阶段按钮。
- 底部日志始终可见并具有足够高度，追加真实阶段、结果和异常；不能使用假进度或结束后才一次性打印全部过程。
- 成功后在日志区域突出显示可复制的网址或目标服务器启动命令，网址支持打开。多组件汇总各组件入口；全部必要组件和整应用检查成功后才显示整体成功。

### 2.2 高级参数与帮助

- 每页提供默认收起的右侧高级侧栏；展开不覆盖主要操作与结果。保留现有高级参数，不因收起改变参数或已保存部署语义。
- 有自定义参数时显示提示；页面、语言和主题切换保留输入及侧栏状态，程序重启默认收起。
- 每个高级参数旁放圆圈问号，鼠标悬停或键盘聚焦可阅读：用途、何时需要修改、默认/留空行为、修改影响。
- “高级选项”按钮旁同样提供解释。帮助使用白话及示例，禁止仅重复参数名。
- 所有静态 UI 文案必须通过 `MessageCatalog` 及中英文消息映射表读取，包括按钮、帮助、状态、补全选项和 DB 冲突。翻译显示与协议值分离；两个消息表的键和命名参数同步，架构测试拦截直接写入控件的文案。
- 原多组件人工编辑从部署页高级侧栏进入，保留组件及依赖调整能力。

| 页面 | 默认显示 | 右侧高级区域 |
| --- | --- | --- |
| 已部署应用 | 应用列表、状态、访问入口、启动/停止/重启 | 服务标识、运行参数、详细诊断 |
| 服务器 | 服务器列表、连接资料、保存并检查连接 | SSH 端口、凭据存储、环境详情 |
| 备份与恢复 | 备份/恢复/迁移任务选择与必要输入 | 归档检查、候选状态、资源和技术详情 |
| AI 配置 | 接口地址、模型、密钥、保存 | 多 Provider、角色分配、调用诊断 |
| 设置 | 语言、主题、使用说明 | 诊断路径、技术信息 |

备份、恢复主操作自动衔接检查和准备；停写、覆盖等必要确认仍明确展示。

## 3. 自动流程、缺项与 AI

流程：固定源码快照 → 识别应用及组件 → 补齐信息 → 检查服务器 → 准备运行环境和数据库 → 构建、初始化、启动 → 健康检查 → 展示结果。

应用服务层新增统一编排入口；UI 仅收集输入和呈现进度。复用类型化分析器、部署计划、远端固定协议、失败恢复与受管持久化，不创建第二套部署引擎。项目清单、声明、已有运行时建议是识别入口、端口及组件关系的依据；多个候选或冲突不得任意选择。

点击“一键部署”授权本次常规环境准备和发布，首次主机信任及数据库替换等高风险操作另外询问。指纹变化继续拒绝。后台执行不得阻塞 Swing 事件线程；重复点击不能发起重复任务。任务期间固定输入，补齐后重新校验并继续同一次任务，取消停止后续步骤。

### 3.1 缺项处理

1. 已接入 AI 时先请求补全。有源码依据、通过确定性校验的非秘密参数自动采用。
2. 未配置 AI、超时、返回无效建议或仍有缺项时弹窗；多个缺项自动组织成表单。
3. 弹窗右侧可展开 AI 问答，解释当前问题、填写方法和示例；未配置时提供配置入口并保留表单。
4. AI 问答不能执行命令；密码、卸载授权、无依据参数不能由 AI 代填或批准。上下文脱敏，不发送秘密和完整源码。
5. 失败、取消、恢复中、等待人工处理分别表达；失败不能显示成功入口。

最小接口：统一部署请求与结果、缺失字段与回答、结构化 AI 建议及问答、服务器列表、真实阶段进度通知。所有数据继续通过现有类型、路径、资源归属与秘密边界校验。

## 4. DB 管理

### 4.1 结构与范围

DB 属于技术生态，各共享模块统一使用 `ecosystem.db`，在 `ecosystem` 内与语言分组并列，不在模块根包新增 `db`。模型、分析、远程契约与 SSHD 实现仍各守模块职责，不放入桌面本地 SQLite 存储模块。Java 包名小写，展示名可以为 DB：

- `ecosystem.db.sql`：PostgreSQL、MySQL、MariaDB。
- `ecosystem.db.document`：文档型分类；本轮不实现 MongoDB 自动安装，也不创建空适配器。
- `ecosystem.db.other`：Redis。

分析、领域模型、Linux 契约和 SSHD 执行分别留在既有对应模块，遵循功能分组规范，不新增独立 Maven 模块。

### 4.2 检测、安装与复用

默认直接安装到所选服务器系统，使用受支持发行版的固定适配，不默认使用容器。

| 服务器状态 | 行为 |
| --- | --- |
| 已有同类型且兼容版本 | 复用，不重新安装；缺少连接/管理凭据时弹窗 |
| 已安装但停止运行 | 检查现有实例，不认定为未安装 |
| 多个实例 | 弹窗选择复用目标 |
| 已有版本不兼容 | 暂停，展示现有版本、项目要求及替换影响 |
| 未安装所需类型 | 安装兼容版本，创建应用数据库/账号或 Redis 访问配置 |
| 类型冲突、来源或版本不明确 | 说明并询问，不静默替换 |

版本优先遵循项目声明。未声明时优先复用已安装兼容版本，否则使用适配器支持的系统仓库版本。不得自动替换数据库种类，不把较新版本简单视为不兼容。生成的凭据通过现有秘密存储保存，仅将引用注入配置；不得进入日志或 AI 上下文。

### 4.3 用户批准旧版本替换

- 备份由用户自行完成。必须确认已备份、已做好停机准备并明确同意卸载旧版、安装目标版。
- 审批绑定服务器、实例及版本组合，执行前重新检查；事实变化使审批失效。
- 不自动清空旧数据目录，不自动恢复用户数据。需要恢复时等待用户恢复，再检查并继续部署。
- 替换软件包与恢复数据是不同状态，不得显示“安装完成”等于“数据恢复完成”。安装和升级受数据库支持路径限制：[PostgreSQL](https://www.postgresql.org/docs/current/upgrading.html)、[MySQL](https://dev.mysql.com/doc/refman/8.4/en/upgrade-paths.html)。

### 4.4 新库初始化与已有库保护

- 只对本次创建、归属明确、可证明为空的应用库自动初始化。
- 支持明确选定的 SQL 初始化文件和受支持运行时中声明的初始化入口；无法确定入口时补问。
- 现有源码迁移标记全局拒绝改为结合目标库状态及初始化计划判断，不能简单移除安全检查。
- 初始化使用限定于应用库的权限，保存执行记录；失败重试不得重复初始化已有数据。
- 已有库结构变更需独立确认，不能冒充新库初始化。
- Redis、多数据库依赖均进入资源清单。现有备份无法覆盖时明确拒绝“完整备份”，不得遗漏资源后声称完整。

## 5. 分阶段实施与验收

| 阶段 | 验收要求 | 状态 |
| --- | --- | --- |
| 文档 | 全部用户决定在代码开发前落档，文档入口与迁移后引用正确 | 已完成：本地 Markdown 链接 0 失效 |
| 界面 | 草图布局、默认收起侧栏、圆圈问号、其他页面简化、状态保留 | 已实现：七页闭合/展开布局、中英文及主题状态回归；最终检查见下文 |
| 编排 | 本地/Git、单/多组件一次启动，真实日志与真实终态，防重复提交 | 已实现：复用 Reviewed 事务；离线验证单/多结果、取消、图冲突和 Git 默认分支固定 |
| AI | 可用/未配置/超时/错误建议/多缺项表单、可展开问答及秘密边界 | 已实现：源码候选校验、表单补填与问答；真实 Provider 调用未作为本轮验收 |
| DB | 四种服务安装/复用/停止/多实例/冲突/拒绝替换/人工恢复等待 | 已接入自动编排；实例决策与 helper 离线行为测试通过，真实系统操作为 RUNTIME-PENDING |
| 初始化 | 新库成功/失败/重试保护/已有库保护/备份能力边界 | 已实现：受限 SQL、归属/空库复核、内容绑定记录与重放拒绝；Redis/多库完整备份拒绝 |
| 回归 | JDK 21 Maven 门禁、桌面视觉检查、测试环境实机结果分别记录 | 规范复核后本地回归：435 项通过、27 项环境条件跳过；原生 DB 离线 13 项通过；真实环境未配置 |

默认窗口及最小窗口都必须看得到主按钮和日志。语言、主题切换保持未保存参数。真实 SSH、数据库、systemd 与发布验证只在明确配置的测试环境执行；缺少环境时继续标记 `RUNTIME-PENDING`，不将本地测试当作实机成功。交付前停止并验证退出所有本次启动的验证进程。

### 5.1 实现入口与明确边界

- `app/service/contract` 的自动门面和 `definition` 请求/结果/交互契约连接 `deployment/automatic` 编排；UI 不执行远端脚本。真实执行事件持续进入日志。
- `shared/model`、`shared/analyze`、`shared/linux`、`shared/linux-sshd` 的 Java 根包下分别使用 `ecosystem.db` 承载类型、声明分析、原生契约及固定执行。SQL 版本规则在 `model.ecosystem.db.sql`，Redis 前缀规则在 `model.ecosystem.db.other`；文档型只有实际分类枚举，不建立空目录。原生 DB helper 资源随 SSHD 实现位于 `gold/debug/windowstolinux/shared/linux/sshd/ecosystem/db`，测试与加载路径同步迁移。目录迁移保持内容和组装顺序；后续行为修正单独更新固定摘要及发行版脚本快照，见本轮复核记录。
- DB 声明格式及新手操作见[产品说明书](../PRODUCT-MANUAL.md#93-db-自动管理)。初始化首批为显式 SQL 与可唯一确定的 Spring SQL 初始化入口；不执行任意迁移命令。已有库变更独立批准；失败记录需人工检查，不自动重放。
- 容器连接宿主 DB 需要显式、可达的高级连接绑定。未知安装来源、系统仓库不能满足版本或残留配置无法确认时暂停；替换不承诺跨版本数据自动升级。
- Redis 与多数据库均保存为资源绑定，但超出现有完整备份能力时明确拒绝。DB 原生安装与 SQLite 本地持久化职责分离。
- 本次保留开发总纲迁移后的正式位置 `docs/development/DEVELOPMENT.md`，无 Web 前端改动。

### 5.2 初始实现验证记录（2026-09-07，复核前）

- 使用 JDK 21 完成干净构建 `mvn -q clean verify`，收尾代码修改后再次完成完整 `mvn -q verify`。最终生成 133 份 Surefire 报告，共 444 项：417 项执行通过、27 项按环境条件跳过，0 失败、0 错误。没有把旧构建目录残留报告计入结果。
- 原生 DB helper 的 Python 离线行为测试 11 项通过，覆盖替换授权和状态重检、停止实例复用、人工恢复等待、初始化成功/失败/重放拒绝、已有库/非空库保护、受限 SQL，以及密码值与密码文件路径注入。测试未连接服务器或执行系统安装。
- 自动部署回归覆盖单应用、取消、多组件整体成功/失败入口、端口冲突和源码身份；Git 测试验证默认分支固定后不受分支推进影响。AI 测试覆盖可采纳候选、无配置/调用失败、超时及无依据建议回退。
- 七个 Swing 页面完成默认收起/展开截图检查，首页内容尺寸 1180×740 与 1060×680 下按钮可见、日志高度至少 200 像素。验证中英文/明暗主题重建后的状态保留；所有离屏测试窗口均已释放。
- 文案审计修正旧多组件、AI、恢复/迁移状态和清理警告直出；DB 诊断使用稳定消息键。`MessageCatalogTest` 与 `DesktopTextArchitectureTest` 各 6 项通过，检查双语键/参数一致、直接控件文案、翻译显示与提交值分离及状态消息覆盖。
- 文档 97 个本地链接有效，本次涉及的 86 个生产文件与目录索引一致；`git diff --check` 无错误。用户已有的开发总纲迁移已保留，没有 Git 提交或 Web 业务修改。
- 本次未配置真实 SSH 测试凭据，因此当前 helper、数据库安装/版本替换/数据恢复、systemd 与完整发布继续标记 `RUNTIME-PENDING`。历史 helper v3 实机证据不能代替本轮验收。
- 已确认本次 Maven、Python 和桌面验证进程全部退出；保留用户 IntelliJ 的后台进程。没有遗留临时服务或预览进程。

### 5.3 规范复核与整改记录（2026-09-07）

本节记录按第 2—4 节复核后确认的差异及修正，第 5.2 节保留初始验证记录，不代表整改后的最终结果。

- **目录与职责**：共享 DB 的 Java 包、镜像测试及 helper 资源统一位于 `ecosystem.db`；交互字段继续使用稳定的 `db/...` 标识，避免把 Java 包迁移误用到表单协议。修正 `File.md` 目标树中开发总纲的旧位置及遗漏的 `contract/definition`、`deployment/automatic`。`AllFile.md` 补齐 14 个遗漏生产文件，移除 6 个误收录测试条目；最终覆盖 733 个非测试维护文件。
- **自动流程与纠错**：运行参数、普通配置、秘密引用和 DB 范围先做本地校验；整应用健康代表组件及源码侧 DB 缺项在检查服务器前确定。纠错表单包含无效高级参数及缺失的手工 DB 详情，可在同一任务继续。显式健康检查选择即使地址留空也保留；静态站点使用 TCP、TCP 配置 HTTP 访问网址等组合明确拒绝。
- **页面与后台操作**：高级侧栏递归识别嵌套输入的自定义状态，并在操作期间冻结输入、完成后恢复原启用状态。服务器、AI 和应用秘密保存移到后台；重复操作被阻止，活动任务期间切换外观会提示并恢复选择。服务器清单加载与状态恢复不再丢失选择通知或覆盖未保存编辑；已部署应用展示已保存服务、发布及运行检查详情，并明确区分实时状态。补齐独立参数问号及双语默认值说明；修正缺项弹窗展开尺寸、窄窗口生命周期按钮截断和诊断按钮拉伸。
- **确认与状态保留**：备份创建明确提示可能停写，恢复明确提示目标、归档及替换影响；取消不会开始操作。备份任务、未提交输入和密码副本可随外观重建恢复，状态释放时清除密码副本。输入弹窗保留真实异常类型，错误不再被误报为取消。
- **DB 边界**：无法识别的安装版本进入冲突与重检流程；多个实例无法安全隔离软件包替换时先等待人工处理。SQL 初始化按库声明绑定，避免向其他库套用同一入口；Redis 拒绝 SQL/JDBC 声明。手工连接不能用“无 DB”、错误种类或单连接遗漏源码声明的资源。既有实例身份复核、独立替换与结构变更批准、应用级权限、初始化记录和失败重放拒绝继续保留。
- **验证**：JDK 21 `mvn -q clean verify` 与完整 `mvn -q verify` 通过；最后两项布局修正后，桌面状态、布局、双语文案与包结构定向回归再次通过。139 份干净生成的 Surefire 报告最终共 462 项：435 项通过、27 项条件跳过，0 失败、0 错误。Python 原生 DB 离线测试 13 项通过。七页完成离屏渲染与逐页展开截图检查；首页 1180×740、1060×680 下主按钮和日志可见，缺项弹窗展开保持表单可用。97 个本地文档链接有效，目录索引无缺项或多余生产文件，`git diff --check` 通过。
- **helper 与运行证据**：版本未知处理改变了 helper 内容，固定 SHA-256 更新为 `ad6692acedf1749c663559c67e114c12525660d5eda048e751d6c21fd3ab2fa8`，20 段组装结果独立复算一致，11 个发行版安装脚本快照同步。真实 SSH、DB 安装/替换/恢复、systemd、完整发布和真实 AI Provider 调用未执行，继续标记 `RUNTIME-PENDING`。未改 POM、Web 业务或提交 Git；本次启动的验证进程已全部退出。

## 6. 文档版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 1.0.0-doc-naming | 2026-09-08 | 首次加入文档版本编号，统一四期补充文档的主题化命名、标题和元信息；保留既有需求、实施与验收记录，运行证据边界不变。 |
