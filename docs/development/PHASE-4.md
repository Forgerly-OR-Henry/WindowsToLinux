# WindowsToLinux 四期工程细化文档

## 文档信息

- 阶段基线版本：`2.25.0-managed-restore-migration`
- 文档结构版本：`2.0.0-roadmap-rebaseline`
- 文档状态：**实施中**
- 当前实现：schema v4 精确绑定依赖有序的组件发布 SHA-256、组件秘密修订与应用级并集，配置文档 v2 保存配置、资源、运行时、两级健康和用户 URL 等完整非秘密激活事实。桌面产品入口已组合完整远端备份、只读目标预检、混合端口候选或整应用短停机、数据库恢复、两级健康、正式提交、失败恢复及双服务器离线迁移。迁移要求明确停写批准，成功只等待人工外部流量切换并保留已停止的源端；源端归属保留期间目标本地接管延后。生产维护外部执行器及 helper v5、真实 Linux/数据库恢复迁移证据继续保持 `RUNTIME-PENDING`
- 更新日期：2026-08-22
- 上级文档：[开发总纲](../DEVELOPMENT.md)

## 文档导航

- [项目结构](../File.md)
- [真实环境验收准备](PHASE-4-RUNTIME-ACCEPTANCE.md)
- [一期](PHASE-1.md)
- [二期](PHASE-2.md)
- [三期](PHASE-3.md)
- [五期](PHASE-5.md)

## 1. 四期目标

四期为已受管应用增加数据库与数据一致性、版本化备份、恢复、离线跨服务器迁移，以及 Windows 桌面客户端自身的签名升级和卸载。备份能力服务于恢复和迁移，但“生成归档”“校验归档”“恢复文件”“运行验证”必须是不同状态。

## 2. 数据清单与一致性策略

### 2.1 备份输入

备份开始前必须形成结构化数据清单：当前发布清单、普通配置快照、密钥引用元数据、持久化文件/卷、数据库类型与连接引用、运行身份、系统服务/容器定义、语言运行时、目标架构和恢复步骤。

未知路径、共享目录归属不明、外部数据库没有受支持导出方式或应用无法进入一致状态时，停止备份并说明缺失项；不能用直接复制伪装一致性备份。

桌面 SQLite v11 只从成功部署时仍持有的已审阅请求保存完整非秘密运行时定义、文件绑定、数据库绑定和独立整应用健康探针，并在同一成功事务中把实际使用的不可变普通配置修订及秘密引用与发布身份精确绑定。多组件文件绑定从 `ComponentDataPath` 形成稳定标识；发布时 helper 将其绑定到固定 Linux 受管根，逻辑源码路径只作为发布内符号链接位置，不会变成任意远端路径。数据库审阅支持 SQLite、PostgreSQL、MySQL 和 MariaDB：服务器数据库只保存端点、库名、用户名、TLS 要求和精确密码 `SecretReference`，绝不保存密码值。SQLite 当前只保存文件名，尚无足以证明物理位置的应用相对路径，因此完整远端备份在连接服务器前显式拒绝 SQLite，不猜测目录。数据库状态区分未知与显式空集合，绑定变化进入发布身份摘要。v10 及更旧整应用健康字段保持缺失；v9 及更旧资源字段保持缺失。

桌面第六个 `BackupApplicationFacade` 先执行不连接服务器的受管输入准入检查：逐组件核对整应用图、当前发布、运行时、文件/数据库范围、精确配置、秘密绑定和应用级健康探针，并通过二次读取拒绝状态漂移。输入完整后，用户可选择目标归档并输入独立备份密码及凭据存储主密码；门面自动使用已保存服务器资料，不要求用户重填 SSH 参数。`persistedInputsComplete` 只表示允许继续远端检查，最终成功仍必须完成远端取材、原运行状态恢复、两级健康、归档校验和最终位置复验。

### 2.2 数据库范围

- SQLite：停止写入并使用 SQLite 在线备份 API 或经过验证的一致副本；禁止在活跃写入时直接复制数据库文件。
- PostgreSQL：使用版本匹配的受控逻辑备份/恢复流程，记录服务器和工具版本。
- MySQL/MariaDB：使用版本匹配的一致性逻辑备份/恢复流程，记录事务和非事务表限制。
- 其他或外部托管数据库：默认只记录人工恢复要求，不宣称已备份；新增正式适配必须另行确认。

涉及数据库结构变化的发布必须在本期定义显式迁移前检查、备份点、执行证据和回滚兼容性。不可逆迁移没有经过验证的恢复路径时不得自动发布。

## 3. 版本化备份格式

### 3.1 包结构

备份以单个版本化归档交付，至少包含：

```text
manifest.json          # 格式版本、应用/组件、来源、架构、文件清单和算法
releases/              # 需要保留的发布产物或可重建源码信息
config/                # 普通配置快照
data/                  # 受管持久化文件/卷导出
database/              # 受支持数据库的一致性导出
runtime/               # 运行时、unit/容器定义和能力清单
secrets.enc            # 用户选择包含秘密时的独立认证加密成员
```

- `manifest.json` 使用明确 schema 版本，未知必需字段或未知重大版本时拒绝恢复。
- 每个成员记录大小和 SHA-256 等内容摘要，用于损坏检测和传输校验。
- 普通摘要可以被攻击者连同内容一起重算，因此不得把“摘要匹配”描述为可信来源证明。
- 归档若提供独立数字签名，才可以在签名密钥可信的前提下声明来源验证；没有签名时界面只显示完整性检查结果。

### 3.2 备份秘密

- 默认排除所有原始秘密，只保留不可用于解密的引用元数据。
- 用户选择包含秘密时，必须设置独立备份密码；平台主密码、Windows Credential Manager 和 Web 主密钥不能代替或被导出。
- 使用带独立随机盐和记录参数的 Argon2id 派生备份密钥，以 AES-256-GCM 等认证加密保护 `secrets.enc`。
- 密码、派生密钥和可等价恢复的材料不进入归档、平台数据库、日志或任务事件。
- 旧密码错误、认证标签失败或参数不支持时停止秘密恢复，不返回部分明文。
- 桌面和 Web 通过同一 `shared/backup` 格式交换加密成员，具体密码输入和解密只发生在各自 `secret` 模块。

桌面 `app/secret.crypto` 当前使用严格规范二进制明文载荷保存最多 64 个唯一“秘密标识 + 正修订号”精确引用；相同标识的不同修订可同时存在，重复的精确引用或非规范顺序会被拒绝。秘密值不进入不可清零字符串，认证后仍会严格检查格式、UTF-8、长度、顺序、精确唯一性和尾随字节。service 对 schema v4 只在全部解码成功且精确引用集合与 manifest 完全一致时返回一个整体可关闭的 `ResolvedSecretRevision` 文档；对 schema v3 只执行旧标识集合认证，不据此授权自动激活。错误密码、认证失败、格式错误、缺少成员或引用不一致均不返回部分秘密，并清零密码、关闭部分修订、清理本次候选。

桌面备份页面现可显式选择“验证密码并准备”：密码输入框立即清空，字符数组由 service 在成功或失败后清零；UI 后台任务只读取已认证修订数量，并在回到 Swing 事件线程前关闭整个秘密文档。页面只接收不含秘密的候选及数量，`BackupPageState` 不保存密码、解码修订或秘密引用。该结果只证明所选本地归档的密码与完整加密成员匹配，不代表秘密已传输或远端恢复已执行。

### 3.3 归档安全

创建和恢复必须限制成员数、单成员/总解压大小、路径长度和压缩比，并拒绝绝对路径、`..` 穿越、越界符号/硬链接、特殊设备、重复覆盖和大小不符。先在受管候选目录完整校验，再允许进入恢复流程。

## 4. 备份流程

桌面当前通过独立 `BackupApplicationFacade` 提供不会连接服务器的本地产品入口：完整校验所选归档，或重新校验后在平台持有的 `work/restore-candidates` 下创建新的摘要绑定隔离候选，并可删除页面当前持有的精确候选。候选根在安全提取前必须不存在；工作区签发不可伪造尝试并绑定父目录创建时文件身份，删除前重新核对工作区、精确父子关系、目录类型和身份，重建对象、工作区外路径或外部替换均失败关闭且保留内容。页面一次只跟踪一个当前候选，删除失败保留身份以供重试，语言或主题重建携带同一内存删除授权；应用重启后不猜测或扫描旧目录。界面明确本地准备或删除都不等于远端恢复或激活。

完整归档产品入口先在同服务器互斥锁下权威观察全部组件，只停止原本运行的组件；随后按依赖顺序收集发布树、普通文件 PAX TAR、命名卷 PAX TAR、Podman OCI Archive 和固定数据库逻辑制品。Docker 没有可验证 OCI 导出时保留已审阅源码发布树用于目标重建，不伪造镜像副本。无论取材成功或失败，入口都会恢复原本运行的组件并执行逐组件和整应用健康；无法证明恢复时返回 `MANUAL_RECOVERY_REQUIRED`。`app/windows.workspace` 在固定工作根保存本次成员，再由用户目标同目录临时文件完成写入、完整校验、无覆盖原子移动和最终路径独立复验；目标已存在、成员摘要不一致或外部替换均失败关闭且不越界删除。

```text
查询受管身份和实时状态
→ 生成数据/数据库/秘密清单
→ 选择并展示一致性策略及停机影响
→ 停止写入或进入经过验证的一致性模式
→ 导出数据库和文件
→ 创建归档、摘要和可选加密秘密
→ 在独立位置重新读取并校验
→ 恢复应用原运行状态
```

- 同服务器重装前必须确认至少一个已完整读取校验的副本位于本机之外或另一服务器。
- 备份失败时恢复原运行状态；恢复失败进入人工处理并保留中间证据。
- “备份创建完成”不等于“恢复演练通过”，界面分别显示创建、完整性、解密可用性和恢复验证状态。

## 5. 恢复

### 5.1 前置检查

恢复前校验格式、摘要/签名状态、备份密码、目标发行版/架构、运行时、数据库版本、端口、路径、空间和现有受管应用冲突。源码型备份优先在目标机重新推导环境并构建；二进制恢复必须展示兼容性结论。

### 5.2 候选恢复

- 所有内容先进入新的受管候选目录和临时数据库/命名空间，不能直接覆盖当前成功版本。
- 数据库和文件恢复完成后启动候选，执行组件健康和整体业务健康。
- 只有全部验证成功才提交当前指针；失败则删除明确归属的候选或保留诊断，不破坏现有版本。
- 明确不兼容的二进制不提供强行启动；跨发行版/主版本的未知兼容只能在用户确认后作为试验候选。

当前平台无关实现使用同一操作标识依次记录前置检查、文件暂存、候选数据库、组件健康、整体健康、提交和恢复证据。schema v4 在 schema v3 的源发行版/版本、14 种封闭运行时、依赖顺序、定义成员引用、组件探针和整应用健康门之上，要求每个组件携带发布 SHA-256 与精确秘密修订；应用级 `releaseSetSha256` 由固定域、组件数量和依赖顺序中的“组件标识 + 发布 SHA-256”按长度分隔计算，应用秘密集合必须等于组件引用并集。恢复适配器把这些身份交给 deploy，deploy 独立重算摘要与并集后才接受请求。未知运行类型、额外命令字段、乱序依赖、错误摘要、非规范秘密引用或缺失定义成员直接拒绝。schema v3 仍可确定性读取、本地检查、准备候选并认证旧秘密标识，但在任何远端暂存或激活前失败关闭；schema v4 声明秘密却排除固定唯一 `secrets.enc` 时同样不能自动激活。源码恢复允许在已验证能力下按目标架构重建，二进制恢复要求架构精确一致，发行版或运行时差异只能在显式试验批准后进入候选。数据库恢复一旦开始，即使适配器尚未返回成功证据也执行幂等候选清理；文件清理或现有发布复核不完整时终态固定为 `MANUAL_RECOVERY_REQUIRED`。

当前具体实现已将 schema v4 归档成员、配置文档 v2 及精确激活身份单向映射到 deploy 和 Linux 恢复契约；SSHD 只创建摘要派生候选目录，经 SFTP 上传精确成员，并再次回读逐项核验大小和 SHA-256，暂存过程不寻址当前发布。目标预检在任何写入前检查受管冲突、能力、空间、架构、数据库工具/版本与端口策略。容器以及静态站点、PHP、Ruby 的显式监听端口可在 `49152-65535` 分配回环候选端口；其余运行时使整应用采用短停机，不允许用新旧混合图冒充健康。秘密、配置和制品暂存后才执行数据库实际连接及候选恢复；候选两级健康仅是提交前预检，正式提交恢复正式端口和服务身份后再次执行组件与整应用健康。任一失败必须清理候选并验证旧发布恢复，否则进入 `MANUAL_RECOVERY_REQUIRED`。远端正式健康后才登记目标秘密和本地整应用图，并单独报告本地接管是否完成或延后。以上为产品组合静态实现，真实 helper v5、Linux 和数据库执行仍为 `RUNTIME-PENDING`。

## 6. 离线一致迁移

迁移采用明确停写窗口，不声称在线无损切换：

```text
预检查目标机并完成初始传输/构建
→ 用户确认停机窗口
→ 停止源应用写入并确认静止
→ 生成最终数据库和文件一致性增量/快照
→ 校验并恢复到目标候选
→ 启动目标并执行整体健康验证
→ 用户手动切换 DNS、代理或其他外部流量
→ 用户确认目标业务正常后结束
```

- 系统不自动接管 DNS、域名、负载均衡或云安全组。
- 目标验证失败时停止目标候选并重新启动、验证源应用。
- 外部流量切换后失败时，界面给出回切步骤，但必须由用户确认外部状态后执行源端恢复。
- 未得到用户确认不得自动删除源端应用、数据或旧版本。
- 迁移结果分别记录源停止、最终同步、目标恢复、运行验证和外部切换状态。

桌面双服务器产品用例把目标归属、能力、端口、兼容性和两副本空间检查置于任何目标写入之前；先从仍运行的源端创建完整初始备份，只有用户明确批准停写窗口后才停止源应用并证明无活跃写入，再创建最终停写完整归档。最终归档摘要绑定目标候选并复用同一受管恢复链。目标正式健康完成后只返回 `READY_FOR_MANUAL_TRAFFIC_SWITCH`：源端保持停止但不删除，最终归档保存在 `data/backups`，系统不修改 DNS、代理或其他外部流量。源端整应用图仍作为权威归属保留时，目标本地接管明确标记 `DEFERRED_SOURCE_RETAINED`，不制造两个“当前”图。任何失败都分别验证目标候选清理和源端恢复；未批准停写时不建立 SSH 会话或执行远端操作。以上为产品组合静态实现，真实双服务器执行仍为 `RUNTIME-PENDING`。

## 7. 桌面客户端升级

### 7.1 更新来源和签名

- 更新元数据和安装包必须通过固定发布信任根验证数字签名与版本，不接受仅有 HTTPS 或摘要的未签名包。
- 禁止降级、未知签名、过期/撤销发布和架构不匹配；紧急回退必须使用同样签名且经用户确认。
- 下载先进入受管临时目录并完整校验，不能覆盖正在运行的程序。

### 7.2 受控替换

```text
停止接收新任务
→ 等待任务进入安全终态或可恢复状态
→ 备份应用 data/SQLite 和版本信息
→ 主程序退出
→ 独立更新器替换程序文件
→ 执行版本化数据库迁移并启动健康检查
→ 成功提交；失败则恢复旧程序和旧数据库
```

更新器不接触明文秘密或实现另一套加密逻辑。`data` 固定位置和用户选择的凭据存储模式升级前后保持一致。数据库回滚必须使用升级前备份，不能尝试让旧程序读取已失败迁移的新结构。

当前安全核心使用严格三段式版本、固定架构、签名有效期、撤销集合和 Ed25519 公钥策略验证已暂存包；摘要只证明内容绑定，只有固定信任根签名通过才产生更新验证证据。等版本和未批准降级拒绝；紧急回退同时需要签名清单标记与用户明确批准。事务已拆分为主进程 `prepare` 和独立更新器 `apply`：前者只停收任务并成对备份程序/SQLite，后者只有在验证更新器身份、主进程退出和交接真实性后才允许替换。主进程交接已可编码为严格有界、版本化、用途隔离且经 HMAC-SHA256 认证的文档；认证通过前不解析领域载荷，错误密钥、篡改、截断、跨用途重放和认证后畸形载荷均失败关闭。编解码器不持有调用方密钥；生产密钥交付、交接文件位置/ACL 和一次性消费仍由独立更新器规格决定。数据库迁移或新版本健康失败仍必须同时恢复旧程序与迁移前 SQLite。仓库未嵌入测试私钥或伪造生产公钥，真实发布信任根和独立更新器仍待接线。

## 8. 卸载

- 每次卸载都要求用户明确选择“保留数据与凭据”或“删除本应用数据与凭据”，不设置默认选项。
- 删除前停止本应用任务并校验 jpackage 安装根、固定 `data` 目录、受管标记和专用 Credential Manager 命名空间。
- 不删除用户项目源码、下载到其他位置的备份、目标 Linux 应用或其他程序数据。
- 删除失败时列出未删除的准确项目，不用“卸载成功”掩盖残留。

当前卸载核心的请求使用可空显式决定而非默认枚举；未选择时不调用任何平台端口。凭据范围固定为唯一 `WindowsToLinux/*`，请求不能改为任意子命名空间。`app/secret` 已用固定 P/Invoke 实现单键删除和命名空间枚举删除：只删除符合应用生成规则的键，命名空间内其他目标作为精确残留返回。主进程 `prepare` 仍只验证决定并停止本应用任务；其交接使用与更新不同的认证用途，防止合法更新文档被卸载路径重放。外部执行器 `apply` 必须验证自身身份、主进程退出和交接真实性后再调用这些能力。真实独立卸载器、认证密钥交付和交接文件 ACL 尚未接线，因此不宣称产品卸载可用。

## 9. 实施顺序

1. 冻结数据清单、数据库适配契约、备份 schema 和兼容策略。
2. 实现归档安全、摘要、签名状态和密码派生/加密边界。
3. 逐项实现 SQLite、PostgreSQL、MySQL/MariaDB 一致性流程。
4. 已实现候选恢复和离线迁移状态机及桌面产品组合；真实失败回切演练待环境验收。
5. 已实现签名更新、应用数据库迁移回滚和卸载安全核心；独立 Windows 执行器待后续讨论。
6. 在桌面端完成真实环境端到端验收；五期仅复用已验证规则。

## 10. 四期不包含

- 在线双写、数据库复制集、自动 DNS/代理切换或零停机迁移。
- 未列出的数据库正式备份适配。
- 官方云备份存储、密钥托管或替用户保管备份密码。
- Web UI、Web 后端升级器、多用户或 SaaS。

## 11. 验收标准

### 11.1 备份与数据库

- [x] SQLite、PostgreSQL、MySQL/MariaDB 各自具有一致性成功和失败恢复静态测试；真实数据库执行仍为 `RUNTIME-PENDING`。
- [x] 活跃数据库不会通过直接复制数据目录被标记为已验证备份。
- [x] 归档路径穿越、链接、特殊文件、压缩炸弹、重复覆盖和篡改被阻止。
- [x] 结果正确区分创建、损坏检测、来源验证、解密和恢复运行验证。
- [x] 桌面固定存储结构只从 `File.md` 运行模式解析的 `data` 根派生 `windowstolinux.db`、`managed-applications`、`work`、`backups` 和 `diagnostics`，不写死 `/var/lib`、盘符、用户目录或临时目录，也不静默回退。
- [x] SQLite v11 为新成功单组件与多组件保存完整运行时、稳定文件/数据库绑定、精确配置、秘密引用及应用级健康探针；数据库未知、显式为空和历史缺失保持可区分，密码值不进入数据库。
- [x] 桌面备份门面可在任何远端操作前结构化核对本地持久化输入，区分应用级与组件级缺失，并在图或发布状态漂移时失败关闭；完整结果不冒充远端归档就绪。
- [x] 完整备份产品入口通过 helper v5 固定受管路径和归属创建 PAX TAR、Podman OCI 与数据库制品；默认统一短停写，失败恢复并复核原运行状态，Windows 端同目录无覆盖原子发布并在最终路径独立复验。真实 Linux/数据库执行保持 `RUNTIME-PENDING`。
- [x] 本地未激活候选具有工作区签发的精确删除生命周期；重建对象、工作区外路径和外部替换不会被删除，页面一次只跟踪一个候选且外观/语言重建保留其删除授权。应用重启后的旧候选不自动扫描或清理。
- [x] schema v4 为每个组件保存发布 SHA-256 和精确秘密修订，应用发布集合按依赖顺序规范摘要且应用秘密集合等于组件并集；deploy 独立复核相同绑定。schema v3 只保留本地检查、候选准备和密码认证，v4 排除所声明的 `secrets.enc` 时也不能自动激活。
- [x] 配置文档 v2 完整保存恢复所需的非秘密配置、资源绑定、运行时、两级健康和用户 URL；恢复产品入口在零写入预检后执行暂存、候选/短停机、数据库恢复、正式提交及失败恢复。真实目标机执行仍为 `RUNTIME-PENDING`。

### 11.2 秘密和迁移

- [x] 独立备份密码通过 Argon2id 派生，`secrets.enc` 认证加密，密码/密钥不落盘。
- [x] 错误密码和认证失败不产生部分秘密或泄露差异信息。
- [x] 桌面可在本地认证独立备份密码与完整秘密修订集合；成功后在 UI 回调前关闭并清零解码秘密，页面状态不保存密码或秘密，且不将认证成功冒充远端恢复。
- [x] 离线迁移产品入口在最终同步前要求明确批准并停止源写入，生成最终停写归档；目标失败必须恢复并验证源应用，成功只等待人工切流且保留源端。真实双端执行仍为 `RUNTIME-PENDING`。
- [x] 外部流量只由用户切换，系统不自动删除源端。

### 11.3 桌面升级与卸载

- [x] 未签名、篡改、错误架构和不允许降级的更新被拒绝。
- [x] 更新核心要求程序替换、SQLite 迁移或启动失败均恢复旧程序与旧数据库；生产独立执行器仍为 `RUNTIME-PENDING`。
- [x] 卸载删除范围被受管标记限制，不具备用户项目、独立备份或远端删除能力；生产独立执行器仍为 `RUNTIME-PENDING`。
- [x] Credential Manager 删除范围固定为 `WindowsToLinux/*`；只删除合法应用键并报告准确残留，测试不清空真实命名空间。
- [x] 更新/卸载主进程交接使用不同认证用途和严格版本载荷；错误密钥、篡改、截断、跨用途及认证后畸形载荷失败关闭，且编解码器不持有调用方密钥。

## 12. 版本记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| 2.25.0-managed-restore-migration | 2026-08-22 | 接通受管恢复和双服务器离线迁移桌面产品组合：配置文档 v2 保存完整非秘密激活事实；目标零写入预检后暂存配置、秘密和制品，执行混合端口候选或整应用短停机、数据库恢复、两级健康、正式提交及失败恢复。迁移创建初始备份，在明确停写后创建最终归档并恢复目标；成功保持源端停止、保留且等待人工切流，源端归属保留时目标本地接管延后。完整 28-POM JDK 21 离线门禁通过 424 项测试、0 失败、0 错误、25 项真实环境条件跳过，131 份 Surefire 报告。真实 helper v5、Linux/数据库及双服务器执行仍为 `RUNTIME-PENDING`，Windows 独立维护执行器待后续讨论。 |
| 2.24.0-managed-remote-backup | 2026-08-22 | SQLite 升至 v11 并原子保存整应用健康探针；普通文件进入固定受管根，容器命名卷和镜像使用可验证归属。helper v5 提供固定 PAX TAR、OCI Archive 和数据库取材/流式回读/精确清理协议；桌面完整备份入口执行默认短停写、原运行状态恢复、两级健康、独立秘密加密及 Windows 无覆盖原子发布。完整 28-POM JDK 21 离线门禁通过 397 项测试、0 失败、0 错误、25 项真实环境条件跳过，126 份 Surefire 报告；真实 Linux/数据库仍为 `RUNTIME-PENDING`。 |
| 2.23.0-managed-resource-bindings | 2026-08-22 | 固定桌面 `data` 根下的 SQLite、受管应用文件/数据库、工作、备份和诊断结构；SQLite v10 为成功部署原子保存稳定文件绑定及 SQLite/PostgreSQL/MySQL/MariaDB 非秘密连接审阅状态，服务器密码只保留精确秘密引用，数据库未知与显式为空以及 v9 旧图缺失保持可区分。绑定变化进入发布身份摘要，备份准入在远端访问前报告资源或数据库范围缺失。完整 28-POM JDK 21 离线门禁通过 393 项测试、0 失败、0 错误、25 项真实环境条件跳过，124 份 Surefire 报告；远端物理采集、候选端口和完整归档入口仍为 `RUNTIME-PENDING`。 |
| 2.22.0-schema-v4-activation-bindings | 2026-08-22 | 将备份写入格式升级为 schema v4：逐组件绑定发布 SHA-256 与精确秘密修订，应用发布集合使用依赖顺序、固定域和长度分隔摘要，应用秘密集合必须等于组件并集；deploy 独立复核相同身份。schema v3 保留确定性读取、本地检查、候选准备和密码认证，但协调器和 Linux 适配器公开入口均在远端暂存前失败关闭；v4 排除已声明的 `secrets.enc` 时也不授权自动激活。JDK 21 完整 28-POM 离线门禁通过 380 项测试、0 失败、0 错误、25 项真实环境条件跳过，121 份 Surefire 报告。 |
| 2.21.0-secret-authentication-ui | 2026-08-22 | 接通桌面本地 `secrets.enc` 密码认证：输入框立即清空，service 对调用密码成功或失败均清零；UI 后台任务在返回前关闭已解码秘密文档，只向页面传递不含秘密的候选和修订数量。页面状态无密码或秘密字段，错误密码、篡改和清单引用不一致继续失败关闭并清理候选。直接测试确认关闭后修订值全零且输出只含公开证据；JDK 21 完整 28-POM 离线门禁通过 369 项测试、0 失败、0 错误、25 项真实环境条件跳过，120 份 Surefire 报告。 |
| 2.20.0-managed-input-ui | 2026-08-22 | 将受管备份持久化输入评估接入桌面页面：按应用/组件显示整应用图、当前发布、已审阅运行时、数据路径、精确发布配置和秘密引用绑定的缺失原因，完整结果仍明确提示远端物理数据、数据库及服务定义未知。页面状态保存应用标识、输出和当前候选；直接呈现测试覆盖完整与多缺失结果。JDK 21 完整 28-POM 离线门禁通过 368 项测试、0 失败、0 错误、25 项真实环境条件跳过，120 份 Surefire 报告。 |
| 2.19.0-local-candidate-lifecycle | 2026-08-22 | 补齐本地未激活候选的受管删除：工作区尝试不可由 service/UI 重建并绑定父目录创建时文件身份，删除前重验范围、关系、目录类型和身份，伪造或外部替换失败关闭。桌面一次只跟踪一个候选，删除失败可重试，语言/主题重建保持授权；应用重启后不扫描未知旧目录。JDK 21 完整 28-POM 离线门禁通过 366 项测试、0 失败、0 错误、25 项真实环境条件跳过，119 份 Surefire 报告；远端激活仍受 schema 与候选端口规格阻塞。 |
| 2.18.0-local-backup-publication | 2026-08-22 | 新增已取材归档的 Windows 安全发布闭环：目标同目录临时文件绑定身份，写入后完整校验，无覆盖原子移动后在最终路径独立复验；既有目标拒绝覆盖，失败只删除本次仍精确持有的临时文件，或文件身份和预发布摘要均未改变的发布文件，外部替换保持原样。聚焦测试覆盖成功、既有目标、流摘要不一致、精确清理和外部替换；JDK 21 完整 28-POM 离线门禁通过 364 项测试、0 失败、0 错误、25 项真实环境条件跳过，119 份 Surefire 报告；远端物理数据/数据库采集及完整产品入口仍待映射。 |
| 2.17.0-managed-backup-input-readiness | 2026-08-22 | 第六个桌面备份门面新增只读受管输入准入，逐组件核对整应用图、当前发布、完整已审阅运行时、数据路径状态、精确配置与秘密绑定；历史缺失使用稳定枚举，二次读取发现图或发布改变时失败关闭。JDK 21 完整 28-POM 离线门禁通过 360 项测试、0 失败、0 错误、25 项真实环境条件跳过；该结果只表示本地持久化输入完整，不访问远端、不创建归档，也不替代物理数据和数据库映射。 |
| 2.16.0-single-component-backup-input-persistence | 2026-08-22 | 单组件 Reviewed 成功部署改用与多组件相同的 SQLite v9 整应用图事务，原子保存完整已审阅运行时、显式空逻辑数据路径、实际发布配置及精确秘密引用；缺失秘密修订时应用、发布、绑定和图整体回滚。JDK 21 完整 28-POM 离线门禁通过 357 项测试、0 失败、0 错误、25 项真实环境条件跳过；容器受管卷仍由运行时精确保留，远端物理文件/卷/数据库映射及完整归档创建不据此宣称完成。 |
| 2.15.0-authenticated-maintenance-handoff | 2026-08-22 | 新增桌面更新/卸载共用 HMAC-SHA256 认证信封和用途隔离的严格版本载荷，认证成功前不重建路径、决定或更新证据；完整保存操作身份、已验证更新、成对备份、显式卸载决定及准备事件，拒绝错误/弱密钥、篡改、截断、跨用途、未知版本、尾随与认证后畸形输入。路径、标识、摘要和证据均补齐可编码边界，编解码器不持有调用方密钥。JDK 21 完整 28-POM 离线门禁通过 355 项测试、0 失败、0 错误、25 项真实环境条件跳过；生产公钥、独立执行器身份、认证密钥交付、交接文件 ACL/一次性消费和实际更新/卸载仍为 `RUNTIME-PENDING`。 |
| 2.14.0-reviewed-backup-input-persistence | 2026-08-22 | SQLite 升至 v9；新成功部署把分析阶段已有的 `ComponentDataPath` 与运行时和整应用图原子保存，并把实际使用的不可变配置修订与发布身份精确绑定；秘密修订绑定可按发布精确读取并区分显式空集合与历史缺失。数据路径载荷严格拒绝未知、截断、尾随、非法模式、非规范顺序及重复路径；新部署显式空清单与 v8 旧图缺失值不同，发布绑定不可改写且 v7/v8 历史发布保持缺失。完整 28-POM JDK 21 离线门禁通过 347 项测试、0 失败、0 错误、25 项真实环境条件跳过；远端物理路径映射和完整归档创建仍未据此宣称完成。 |
| 2.13.0-credential-namespace-deletion | 2026-08-22 | 为数据库加密秘密与 Windows Credential Manager 增加精确删除；Credential Manager 只枚举固定 `WindowsToLinux/*`，合法应用键才删除，其他目标保留为准确残留。卸载请求拒绝可变子命名空间；JDK 21 完整 28-POM 离线门禁通过 338 项测试、0 失败、0 错误、25 项真实环境条件跳过，生产独立执行器仍待接线。 |
| 2.12.0-reviewed-runtime-persistence | 2026-08-22 | SQLite 升至 v8，从成功部署请求原子保存 14 种完整非秘密已审阅运行时；编解码有独立版本、集合/文档边界并拒绝未知、截断与尾随载荷。v7 旧图迁移后保持定义缺失，生命周期不受影响，完整归档创建必须明确拒绝而不得猜测。 |
| 2.11.0-phase4-runtime-readiness | 2026-08-22 | 新增真实环境验收准备文档，冻结 JDK 21 静态门禁、helper v4 现有产品入口的环境变量/系统属性/命令和证据模板；逐项标明数据库、恢复、迁移与桌面维护仍缺少的产品接线，明确当前尚不能仅凭测试服务器完成四期端到端验收。 |
| 2.10.0-phase4-desktop-maintenance-handoff | 2026-08-22 | 将更新/卸载主进程准备与外部执行拆为不同 API，任何替换或删除前都要求执行器身份、主进程退出和交接真实性证据，卸载还在外部进程重新验证受管边界。同步 helper v4 发行版脚本测试镜像并通过 JDK 21 完整 28-POM 离线门禁；生产公钥、独立 jpackage 执行器、Credential Manager 和真实环境证据仍为 `RUNTIME-PENDING`。 |
| 2.9.0-phase4-secret-revision-handoff | 2026-08-22 | 定义不产生秘密字符串的严格规范二进制秘密修订载荷；`app/secret.crypto` 在 Argon2id/AES-GCM 整体认证后形成可清零 `ResolvedSecretRevision`，service 再要求秘密标识集合与 manifest 完全一致。任何失败均关闭部分结果、清零调用方密码并清理本次候选；该交接不外推为候选已激活。 |
| 2.8.0-phase4-desktop-backup-entry | 2026-08-22 | 接通 service/main/Swing 的最小备份功能入口：只读完整校验，或重新校验并提取到 Windows 平台工作区的摘要绑定隔离候选；失败局限于本次尝试，语言/主题切换保留页面状态。入口不连接服务器、不激活候选，远端恢复仍受候选端口与解密秘密交接阻塞。 |
| 2.7.0-phase4-typed-restore-staging | 2026-08-22 | 升级 manifest schema v3，封闭保存全部已审阅组件运行时、依赖、定义成员引用和两级健康门；新增 backup 到 deploy/Linux 的单向适配、Linux 恢复成员契约及 SSHD 摘要候选 SFTP 上传/独立回读。缺少候选无冲突端口和解密秘密交接时明确保留激活/切换，不把路径、说明或加密文件转换为命令。 |
| 2.6.0-phase4-desktop-maintenance-core | 2026-08-22 | 实现 Windows 桌面更新清单、Ed25519 固定信任策略、版本/架构/有效期/撤销验证，以及独立更新器交接和程序/SQLite 成对回滚状态机；实现无默认选择卸载、受管边界验证、保留/删除分支与精确残留结果。当前不含生产公钥、独立 jpackage 更新器或 Credential Manager 平台接线。 |
| 2.5.0-phase4-offline-migration-core | 2026-08-22 | 实现平台无关离线迁移窄端口、状态、事件、结果与协调器；强制目标先预检、明确停写、摘要绑定最终同步、目标两级健康、失败源端恢复、人工外部切流和源端保留。区分纯前置拒绝、已清理目标修改、已恢复源端与人工恢复；具体双端远程实现及实机证据仍未完成。 |
| 2.4.0-phase4-candidate-restore-core | 2026-08-22 | 升级 manifest schema v2 并补充源发行版事实；实现受控候选请求、目标预检、源码/二进制兼容规则、隔离文件和数据库组合恢复、两级健康、成功提交、失败保留现有版本和人工恢复终态。候选数据库新增幂等清理固定动词；包依赖保持无环。当前只完成平台无关核心，具体 deploy/Linux 端口、桌面入口和真实环境证据仍未完成。 |
| 2.3.0-phase4-database-adapters | 2026-08-21 | 冻结三类数据库连接、兼容性、导出制品、候选恢复证据与闭合注册表；SQLite 优先在线备份，PostgreSQL 检查服务/工具兼容性，MySQL/MariaDB 区分全事务表与需停写锁定情形。跨模块远程能力经 `linux.protocol.database` 单向契约进入 helper v4，固定动词实现临时凭据、候选数据库和流式制品校验，`linux-sshd` 不反向依赖 `backup`；当前仅有本地静态证据，桌面接线和真实恢复保持未完成。 |
| 2.2.0-phase4-secret-encryption | 2026-08-21 | 实现严格 `secrets.enc` 信封与 `app/secret` 调用级 Argon2id/AES-256-GCM，加密服务不持有密码或密钥，错误密码与篡改统一失败；桌面备份用例尚未接通，故验收勾选保持未完成。 |
| 2.1.0-phase4-archive-core | 2026-08-21 | 落地平台无关归档核心与负向静态测试；只确认清单、写入核验、安全预校验、来源状态和隔离候选提取，数据库适配、秘密加密、迁移、桌面升级、卸载和真实环境证据仍待完成。 |
| 2.0.0-phase4 | 2026-08-08 | 接收原三期备份迁移与桌面升级范围，确定独立备份密码、离线一致迁移和签名升级。 |
| 1.2.0-phase3 至 1.7.0-managed-lifecycle | 2026-08-07 | 相关历史需求原位于三期，现由四期替代。 |
